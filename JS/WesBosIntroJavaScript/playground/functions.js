/*  eslint-disable */

/*
Math.max(10,12)
>>12
---Math.max is a FUNCTION
---12 is the RETURN or RESULTS 

-functions can take in data, this is called an argument
--ARGUMENTS--passing data into a funciton
---(10,12) is the ARGUMENTS

-functions can do things as well this is called a statment
--STATEMENTS--the work prefomred by a function
---Math.max(10,12), all that together is the STATEMENT
*/
